# Archived Diagram Variants

This directory keeps legacy and superseded diagram assets that were previously
stored under `tmp-diagrams/`.

These files are not canonical references for current architecture docs.
Use the diagram set in `../` for active documentation.

